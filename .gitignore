# Dependencies
node_modules/

# Internal working files. REQUIREMENTS.md holds an audit of my own machine,
# so it must never be published.
CONTEXT.md
PLANNING.md
REQUIREMENTS.md
design/
archive/

# Electron build output
dist/
out/

# macOS
.DS_Store
*.plist

# Logs
*.log
npm-debug.log*

# Local config (each user has their own at ~/.station/config.json)
config.json

# Environment
.env
